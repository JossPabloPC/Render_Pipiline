import math

class Vector3:
    def __init__(self, x,y,z):
        self.x = x
        self.y = y
        self.z = z

    def magnitud():
       self.magnitud = math.sqrt(math.pow(x, 2) + math.pow(y, 2) + math.pow(z, 2))